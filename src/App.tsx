function App() {
  return (
    <div style={{ padding: '40px', height: '100%' }}>
      <h1 style={{ fontFamily: 'var(--font-display)' }}>Deep Work</h1>
      <p style={{ fontFamily: 'var(--font-mono)', color: 'var(--text-muted)' }}>
        Tauri v2 + React scaffold ready
      </p>
    </div>
  )
}

export default App
