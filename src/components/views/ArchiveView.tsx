import { ViewPlaceholder } from './ViewPlaceholder'

export function ArchiveView() {
  return (
    <ViewPlaceholder
      title="Archive"
      subtitle="Every day you planned blocks. A dot means the day has a record — open it to see what actually landed."
      phase="Phase 7"
    />
  )
}
