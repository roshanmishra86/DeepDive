import { ViewPlaceholder } from './ViewPlaceholder'

export function TemplatesView() {
  return (
    <ViewPlaceholder
      title="Day templates"
      subtitle="Repetition lives here: build a day once, apply it whenever it fits."
      phase="Phase 6"
    />
  )
}
