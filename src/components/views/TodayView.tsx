import { ViewPlaceholder } from './ViewPlaceholder'

export function TodayView() {
  return (
    <ViewPlaceholder
      title="Today's blocks"
      subtitle="A proportional timeline of the day — block heights scale with duration."
      phase="Phase 4"
    />
  )
}
