import { ViewPlaceholder } from './ViewPlaceholder'

export function WeekView() {
  return (
    <ViewPlaceholder
      title="This week"
      subtitle="A list to remember, not a schedule. Tag by urgency and importance, then pull what today can hold."
      phase="Phase 5"
    />
  )
}
