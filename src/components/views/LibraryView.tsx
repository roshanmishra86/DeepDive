import { ViewPlaceholder } from './ViewPlaceholder'

export function LibraryView() {
  return (
    <ViewPlaceholder
      title="Sound library"
      subtitle="Local mp3 files, loaded from disk. Nothing streams."
      phase="Phase 8"
    />
  )
}
